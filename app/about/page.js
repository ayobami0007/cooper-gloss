export const metadata = { title: "About — Cooper Gloss" };

export default function AboutPage() {
  return (
    <div className="mx-auto max-w-2xl px-4 py-12 font-body text-ink">
      <h1 className="font-display text-3xl mb-6">About Cooper Gloss</h1>
      <p className="text-ink/70 italic">
        Brand story, founder background, and values are still pending from
        the client — do not publish this page with invented content. Replace
        this placeholder once real copy is provided.
      </p>
    </div>
  );
}
